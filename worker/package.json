{
  "name": "tag-stats-worker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "init-db": "wrangler d1 execute tag-stats-db --file=./schema.sql"
  }
}